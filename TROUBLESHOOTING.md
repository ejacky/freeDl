# 故障排除指南

## 无法检测到m3u8 URL的解决方案

### 问题描述
扩展无法检测到页面中的m3u8视频链接，但用浏览器开发者工具可以看到确实存在m3u8 URL。

### 解决步骤

#### 1. 使用调试工具
1. 访问包含m3u8视频的网页
2. 页面右上角会出现"🎬 Video Downloader Debug"调试面板
3. 点击"🔍 手动检测"按钮
4. 查看检测日志，了解具体检测过程

#### 2. 检查浏览器控制台
1. 按F12打开开发者工具
2. 切换到Console标签页
3. 查看是否有"Video Downloader"相关的日志
4. 特别关注以下信息：
   - "开始检测页面中的m3u8 URL..."
   - "找到XXX中的m3u8: URL"
   - "检测到的m3u8 URL: [...]"

#### 3. 手动重新检测
1. 点击扩展图标打开弹窗
2. 点击"🔍 手动重新检测"按钮
3. 等待检测完成

#### 4. 检查页面加载状态
- 确保页面已完全加载
- 如果是动态加载的内容，等待内容加载完成后再检测
- 尝试刷新页面后重新检测

#### 5. 检查网络请求
1. 在开发者工具中切换到Network标签页
2. 刷新页面
3. 搜索".m3u8"查看是否有相关请求
4. 如果看到m3u8请求，扩展应该能够检测到

### 常见检测失败原因

#### 1. 页面使用JavaScript动态加载
- **现象**: 页面初始加载时没有m3u8 URL，通过JavaScript异步加载
- **解决**: 等待页面完全加载，或使用手动检测功能

#### 2. 跨域限制
- **现象**: m3u8 URL在iframe中，但跨域无法访问
- **解决**: 这是浏览器安全限制，无法绕过

#### 3. 加密或混淆的URL
- **现象**: URL经过加密或混淆处理
- **解决**: 扩展无法检测加密的URL

#### 4. 使用WebRTC或其他非HTTP协议
- **现象**: 使用WebRTC、WebSocket等协议传输
- **解决**: 扩展只能检测HTTP/HTTPS协议的URL

### 高级调试方法

#### 1. 使用浏览器控制台手动检测
```javascript
// 在控制台中运行以下代码
window.videoDownloaderDebug.detectAllUrls();
```

#### 2. 检查全局变量
```javascript
// 检查常见的全局变量
console.log('videoUrl:', window.videoUrl);
console.log('streamUrl:', window.streamUrl);
console.log('m3u8Url:', window.m3u8Url);
```

#### 3. 检查页面文本内容
```javascript
// 搜索页面中所有包含m3u8的文本
const text = document.body.innerText;
const matches = text.match(/https?:\/\/[^\s"'<>]+\.m3u8[^\s"'<>]*/gi);
console.log('找到的m3u8 URL:', matches);
```

#### 4. 检查网络拦截
```javascript
// 检查是否有网络请求被拦截
console.log('拦截到的URL:', interceptedUrls);
```

### 如果仍然无法检测

#### 1. 检查扩展权限
- 确保扩展有访问所有网站的权限
- 检查manifest.json中的权限配置

#### 2. 重新安装扩展
1. 在Chrome扩展页面移除扩展
2. 重新加载扩展文件夹

#### 3. 检查Chrome版本
- 确保使用Chrome 88+版本
- 旧版本可能不支持某些API

#### 4. 联系支持
如果以上方法都无法解决问题，请：
1. 记录具体的网页URL
2. 截图显示调试面板的检测结果
3. 提供浏览器控制台的错误信息

### 预防措施

1. **及时更新扩展**: 定期检查扩展更新
2. **使用最新浏览器**: 保持Chrome浏览器为最新版本
3. **耐心等待**: 某些网站需要较长时间加载视频资源

### 技术说明

扩展使用以下方法检测m3u8 URL：
1. 扫描页面中的所有链接
2. 解析script标签中的JavaScript代码
3. 检查页面文本内容
4. 检查HTML元素属性
5. 监控网络请求
6. 检查浏览器存储
7. 检查全局变量

如果m3u8 URL使用了上述方法之外的隐藏方式，扩展可能无法检测到。
